[project]
name = "datapulse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp",
    "sqlalchemy",
    "psycopg2-binary",
    "python-dotenv",
]
